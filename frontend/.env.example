# 浏览器请求后端 API 的地址；同域反向代理时可留空或设置为站点域名。
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# 仅供 Next.js 服务端（RSS、sitemap、metadata）请求后端使用。
# 生产环境可填写内网地址，避免服务端请求绕到公网。
INTERNAL_API_BASE_URL=http://localhost:8080

# 用户实际访问本站的公开地址，用于 canonical URL、RSS 和 sitemap。
SITE_URL=http://localhost:2025
